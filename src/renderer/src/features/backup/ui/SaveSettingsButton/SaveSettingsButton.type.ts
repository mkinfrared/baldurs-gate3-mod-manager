import { Game } from "@renderer/shared/types";

export type SaveSettingsButtonProps = {
  /**
   * a string that will be applied as a css class to parent element
   */
  className?: string;
  game: Game;
};
