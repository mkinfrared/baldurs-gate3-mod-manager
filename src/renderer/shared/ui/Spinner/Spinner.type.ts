export type SpinnerProps = {
  /**
   * a string that will be applied as a css class to parent element
   */
  className?: string;
  type?: "bars" | "oval" | "puff" | "rings" | "ripple" | "tail-spin";
};
